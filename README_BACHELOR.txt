Please make sure to set url(s) in config.json correctly!
All scenes named Bachelor_[...] have distance-based tile-preloading enabled! it might take a while to load the tiles!
When running the 3D-map-tiles services on the same pc, new tile requests WILL impact the rendering performance!
The 3D-map-tiles service takes a while to generate new tiles! it is a lot faster when tiles are cached!